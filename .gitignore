# Cargo (workspace target + custom CARGO_TARGET_DIR e.g. when default target/ is locked)
/target/
/target-*/

# Git worktrees (isolated feature branches)
/.worktrees/
/worktrees/

# Local env and editor
.env
/.cursor

# App runtime artifacts (local dev)
romm-cache.json
/downloads
/cache
/.docs-cache
